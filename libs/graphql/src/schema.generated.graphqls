type Company {
  name: String!
  pk: ID!
}

scalar DateTime

type Mutation {
  createCompany(name: String!): Company!
  deleteCompany(pk: ID!): Company!
  updateCompany(name: String!, pk: ID!): Company!
}

type Query {
  companies: [Company!]!
  company(pk: ID!): Company!
}