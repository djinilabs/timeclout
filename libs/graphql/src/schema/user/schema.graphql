type User {
  pk: String!
  email: String!
  name: String!
  emailMd5: String!
  resourcePermission: Int
  resourcePermissionGivenAt: DateTime
  settings(name: String!): JSON
}

extend type Query {
  me: User
}
