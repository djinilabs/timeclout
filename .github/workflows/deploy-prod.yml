name: Deploy Prod

on:
  workflow_run:
    workflows: ["Test"]
    types:
      - completed
    branches:
      - main

jobs:
  deploy:
    if: ${{ github.event_name != 'pull_request' && github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    concurrency:
      group: deploy-prod
      cancel-in-progress: false

    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: eu-west-2

      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        name: Install pnpm
        with:
          run_install: false

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          architecture: "x64"
          cache: pnpm

      - name: Install
        run: pnpm install -w

      - name: Deploy
        env:
          NODE_ENV: "production"
          BASE_URL: "https://app.tt3.app"
          NODE_OPTIONS: "--enable-source-maps --max_old_space_size=32768"
          AUTH_SECRET: ${{ secrets.AUTH_SECRET }}
          TT3_CUSTOM_DOMAIN: "app.tt3.app"
          TT3_CERTIFICATE_ARN: ${{ secrets.TT3_CERTIFICATE_ARN }}
          TT3_ZONE_ID: ${{ secrets.TT3_ZONE_ID }}
          VITE_SENTRY_DSN: "${{secrets.VITE_SENTRY_DSN}}"
        run: |
          cd apps/backend
          export PATH=../../node_modules/.bin:$PATH
          pnpm arc env --add --env production AUTH_SECRET "${{ secrets.AUTH_SECRET }}"
          pnpm arc env --add --env production AUTH_MAILGUN_KEY "${{ secrets.AUTH_MAILGUN_KEY }}"
          cd ../..
          pnpm build
          cd apps/backend
          pnpm arc deploy --production --no-hydrate --verbose
