query TeamApprovedSchedule(
  $teamPk: String!
  $startDate: Date!
  $endDate: Date!
) {
  team(teamPk: $teamPk) {
    approvedSchedule(startDate: $startDate, endDate: $endDate) {
      pk
      userSchedules {
        pk
        user {
          pk
          name
          email
          emailMd5
        }
        leaves {
          pk
          sk
          leaveRequestPk
          leaveRequestSk
          type
        }
      }
    }
  }
}
