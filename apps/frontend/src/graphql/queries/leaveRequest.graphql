query leaveRequest($pk: String!, $sk: String!) {
  leaveRequest(pk: $pk, sk: $sk) {
    pk
    sk
    createdBy {
      pk
      name
      email
      emailMd5
    }
    createdAt
    startDate
    endDate
    type
    reason
  }
}
