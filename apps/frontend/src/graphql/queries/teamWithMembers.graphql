query teamWithMembers($teamPk: String!, $skills: [String!]) {
  team(teamPk: $teamPk) {
    pk
    name
    members(skills: $skills) {
      pk
      name
      email
      emailMd5
      resourcePermission
      resourcePermissionGivenAt
    }
  }
}
