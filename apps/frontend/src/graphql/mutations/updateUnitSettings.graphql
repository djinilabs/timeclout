mutation updateUnitSettings(
  $unitPk: String!
  $name: String!
  $settings: JSON!
) {
  updateUnitSettings(unitPk: $unitPk, name: $name, settings: $settings) {
    pk
    name
    settings(name: $name)
  }
}
