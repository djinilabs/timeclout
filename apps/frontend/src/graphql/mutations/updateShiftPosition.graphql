mutation UpdateShiftPosition($input: UpdateShiftPositionInput!) {
  updateShiftPosition(input: $input) {
    pk
    sk
    day
    name
    color
    requiredSkills
    userVersion
    schedules {
      startHourMinutes
      endHourMinutes
      inconveniencePerHour
    }
    assignedTo {
      pk
      name
      email
      emailMd5
    }
  }
}
